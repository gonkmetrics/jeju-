<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.jejewn.mapper.CartMapper">
	
	<select id="getAllCartList" resultType="io.jejewn.persistence.CartVO">
		SELECT * FROM cart ORDER BY id DESC
	</select>
	
	<select id="getCartList" resultType="io.jejewn.persistence.CartVO">
		SELECT * FROM cart WHERE card_id = #{cId} ORDER BY id DESC
	</select>
	
	<insert id="insert">
		INSERT INTO cart
		(id, token, user_id)
			VALUES
		(id.nextval, #{token}, #{uId})
	</insert>
	
	<delete id="delete">
			DELETE FROM cart
			WHERE id = #{cId}
	</delete>

</mapper>